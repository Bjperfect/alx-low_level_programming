#include "main.h"
/**
 * _isupper - funtion that check fo uppercase char
 * @c: input
 * Return: 1 if c is upercase, 0 otherwise
 */
int _isupper(int c)
{
if ((c >= 'A') && (c <= 'Z'))

return (1);

else
return (0);
}
